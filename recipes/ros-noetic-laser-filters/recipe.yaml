package:
  name: ros-noetic-laser-filters
  version: 1.8.11
source:
  git_url: https://github.com/ros-gbp/laser_filters-release.git
  git_rev: release/noetic/laser_filters/1.8.11-1
  folder: ros-noetic-laser-filters/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-distro-mutex 0.1 noetic
    - ros-noetic-angles
    - ros-noetic-catkin
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-filters
    - ros-noetic-laser-geometry
    - ros-noetic-message-filters
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-rostest
    - ros-noetic-sensor-msgs
    - ros-noetic-tf
  run:
    - python
    - ros-distro-mutex 0.1 noetic
    - ros-noetic-angles
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-filters
    - ros-noetic-laser-geometry
    - ros-noetic-message-filters
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-sensor-msgs
    - ros-noetic-tf
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
