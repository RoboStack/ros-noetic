packages:
  - libopencv
  # - gdal  # cannot currently migrate because qt then conflicts because of differing icu dependencies
  # - libgdal
  - hdf5
  - vtk
  - proj
  - pcl

packages_ignore:
  - lvr2
  - safety_limiter
  - grid_map_filters
